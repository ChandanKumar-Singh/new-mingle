// const express = require("express");
// const router = express.Router();
// const sequelize = require("../../../config/db");

// const { dispatcher } = require("../../../middleware");
// const { register, registerOtp, registerVerify } = require("../../../controllers/v1");

// router.post("/", (req, res, next) => dispatcher(req, res, next, register));

// // router.post("/send-otp", (req, res, next) => dispatcher(req, res, next, registerOtp));

// // router.put("/", (req, res, next) => dispatcher(req, res, next, registerVerify));


// module.exports = router;
